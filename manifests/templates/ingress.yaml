---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finally                  # Ingress name
  labels:
    app: finally
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "finally.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: finally               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
